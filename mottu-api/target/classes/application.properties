# Server
server.port=8080

# DataSource (Oracle)
spring.datasource.url=jdbc:oracle:thin:@oracle.fiap.com.br:1521:ORCL
spring.datasource.username=rm558514
spring.datasource.password=030904
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# JPA / Hibernate
# Como o banco já existe, melhor não deixar o Hibernate recriar tabelas:
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Desliga Flyway (já que você quer usar o schema existente e removeu as migrações)
spring.flyway.enabled=false
